#系统级别的一些参数配置，这些参数一般是不会变动的
spring:
  application:
    name: service-gateway #服务名
  cloud:
    nacos:
      server-addr: localhost:8848 #nacos服务地址
      config:
        namespace: cd2e1d45-b3ef-4fb5-8149-4bad3f4c431c #nacos配置中心的命名空间id
        file-extension: yaml #nacos配置文件用了yaml这里要注明，否则取不到配置中心的配置文件
